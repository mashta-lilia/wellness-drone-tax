## 🎨 Фронтенд: Інтерфейс Адмін-панелі Wellness Drone Tax
У проєкті реалізований сучасний, швидкий та суворо типізований користувацький інтерфейс для управління даними замовлень та податковими розрахунками доставок дронами.

### Що було реалізовано:
**Сучасний стек технологій**: Застосунок побудовано на базі React та TypeScript із використанням збирача Vite для максимальної швидкодії. Візуальна частина базується на бібліотеці компонентів Material-UI (MUI v6).
**Архітектура та компоненти**: Логіка інтерфейсу розділена на незалежні модулі для легкої підтримки коду (наприклад, виділено ізольований компонент OrderRow для розгортання деталей рядка таблиці та SummaryCard для відображення статистики).
**Модуль імпорту CSV (FileUploader)**:
   * Інтегровано підтримку drag-and-drop через react-dropzone.
    * Додано клієнтську валідацію: перевірка розширення файлу (тільки .csv) та обмеження розміру (до 5 МБ).
    * Реалізовано інформативне модальне вікно (Dialog) із детальним звітом про результати завантаження (успішні рядки та деталізація помилок).
**Дашборд замовлень (OrdersListPage)**: Реалізовано повноцінну таблицю з серверною пагінацією, сортуванням та фільтрацією (за ID та датою). Додано функціонал експорту відфільтрованих даних у формат CSV та можливість безпечного очищення бази.
* **Взаємодія з API**: Налаштовано глобальний екземпляр Axios із перехоплювачами (interceptors):
* **Request Interceptor**: автоматично додає JWT-токен авторизації до кожного запиту.
**Response Interceptor**: глобально обробляє помилки авторизації (401), автоматично очищуючи сесію та перенаправляючи користувача на сторінку логіну.

### 🛠 Як запускати проєкт

Для розробки та запуску фронтенду вам знадобиться встановлений Node.js (рекомендовано v18+).

1. **Налаштування змінних оточення**. Створіть файл `.env` у кореневій директорії папки frontend та вкажіть шлях до вашого бекенду:
    * `VITE_API_BASE_URL=http://localhost:8000/api`
2. **Встановлення залежностей.** Перейдіть у папку frontend та виконайте команду:
    * `npm install`
3. **Запуск сервера розробки.** Знаходячись у папці frontend, виконайте команду:
    * `npm run dev`

**Доступ до сервісів:**

Фронтенд буде доступний за адресою: http://localhost:5173 (або інший порт, який видасть Vite).

### ✅ Як перевіряти працездатність

**Локальна перевірка якості коду:**

* Переконайтеся, що код відповідає стандартам, виконавши перевірку лінтером: `npm run lint`

* Перевірте тестову збірку проєкту на відсутність помилок TypeScript: `npm run build`

**Перевірка через CI-пайплайн:**

* Будь-який новий код, відправлений у гілку main, буде автоматично перевірений пайплайном.

* Перейдіть у вкладку "Actions" на GitHub, щоб переконатися, що кроки Lint Frontend та Build Frontend завершуються без помилок (світяться зеленим).

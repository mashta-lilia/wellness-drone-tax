# 🚁 Instant Wellness Kits Tax API (BetterMe Test Task)

## 📖 Про проєкт (Бізнес-контекст)

Проєкт розроблено в рамках технічного завдання для стартапу, що займається доставкою wellness-наборів дронами в будь-яку точку штату Нью-Йорк. Через стрімке зростання сервісу, компанія упустила питання оподаткування (Sales Tax). Застосунок списував з користувачів лише базову вартість набору (subtotal), ігноруючи податки.

**Головна мета:** Маючи лише координати доставки (latitude, longitude) та базову вартість (subtotal) , автоматично визначити застосовувані юрисдикції та розрахувати композитну податкову ставку (composite tax rate), суму податку та підсумкову вартість для кожного замовлення .

---

## ✅ Реалізований функціонал

Система повністю покриває всі вимоги ТЗ:

* **Import CSV:** Завантаження масиву замовлень через файл, масовий розрахунок податків та збереження в БД.


* **Manual Create:** Ручне створення замовлення за координатами з миттєвим розрахунком та відображенням деталізації.


* **Orders List:** Таблиця замовлень з пагінацією та фільтрацією (за ID та датою).


* **Авторизація (Бонус):** Реалізовано вхід в адмін-панель за допомогою облікових даних адміністратора.


* **Метеорологічна безпека (Бонус):** Інтеграція з Open-Meteo API для перевірки погодних умов у точці доставки (швидкість вітру, опади, температура) в реальному часі для оцінки безпеки польоту дрона.

---

## 🛠 Технологічний стек та архітектурні рішення

Хоча в ТЗ рекомендувався Node.js, для вирішення задачі геопросторового аналізу та обробки великих масивів даних (CSV) був обраний **Python (FastAPI)**.
Це архітектурно обґрунтоване рішення, оскільки екосистема Python (зокрема `Shapely`, `Pandas` та `R-Tree`) на порядки ефективніше справляється з математичними обчисленнями просторової геометрії полігонів та векторизацією даних, ніж JavaScript. Фронтенд реалізовано на **React + TypeScript** у відповідності до вимог.

---

## 🧮 Алгоритм розрахунку податків (Tax Calculation Logic)

Податкова система штату Нью-Йорк має складну структуру. Сервіс розраховує податки, розбиваючи їх на юрисдикції (breakdown):

1. 
**Базова ставка штату (State Rate):** Фіксована ставка для всього штату Нью-Йорк — **4.0%**.


2. 
**Окружна ставка (County Rate):** Варіюється залежно від округу (від 3% до 4.75%). Для визначення округу ми використовуємо GeoJSON меж штату Нью-Йорк.


3. 
**Міська ставка (City Rate):** Специфічний податок міста Нью-Йорк (NYC Local Sales Tax), який становить **4.5%** і діє на території 5 боро (округів міста). У цих округах базова окружна ставка дорівнює 0%.


4. 
**Спеціальні ставки (MCTD):** Додатковий транспортний податок **0.375%**, який застосовується лише до 12 конкретних округів.



---

## 🚀 Етапи розробки та оптимізації продуктивності

Процес розробки пройшов через кілька стадій рефакторингу для досягнення максимальної продуктивності.

### Етап 1: Вирішення геопросторових крайових випадків (Edge Cases)

Часто координати дронів можуть потрапляти на мости, річки або прибережні зони, які формально виходять за суворі математичні межі полігонів. Для вирішення цієї проблеми при завантаженні GeoJSON застосовується **буферизація полігонів (`buffer(0.001)`)**, що розширює їхні межі приблизно на 100 метрів.

### Етап 2: Просторові індекси (R-Tree)

Для кардинального прискорення пошуку юрисдикцій було впроваджено **просторовий індекс R-Tree** з використанням бібліотеки `shapely.strtree`.
Замість перевірки кожної координати через кожен полігон (лінійний пошук), R-Tree моментально відсікає нерелевантні регіони, знижуючи алгоритмічну складність до $O(\log N)$.

### Етап 3: Векторизація розрахунків (Pandas)

Найбільшим викликом був імпорт великих CSV-файлів. Рішення — повна **векторизація обчислень**:

1. CSV-файл завантажується безпосередньо в `pandas.DataFrame`.
2. Координати векторизуються в масив точок `shapely.points()`.
3. Функція `spatial_index.query()` виконується матрично.
4. Розподіл податкових ставок (City vs County) та генерація JSON-структур відбувається через векторні операції Pandas (`np.where`), після чого дані батчем зберігаються в БД.

---

## 🖥️ Як запустити проєкт локально

Проєкт повністю контейнеризовано за допомогою Docker, що забезпечує запуск "в один клік" без необхідності налаштування локального оточення.

### Інструкція із запуску:

1. Клонуйте репозиторій:
```bash
git clone https://github.com/mashta-lilia/wellness-drone-tax.git
cd wellness-drone-tax

```


2. Запустіть збірку та старт контейнерів:
```bash
docker compose up --build

```


3. Після успішного запуску сервіси будуть доступні за адресами:
* **Frontend (UI адмінки):** [http://localhost:3000](https://www.google.com/search?q=http://localhost:3000)
* **Backend API (Swagger UI):** [http://localhost:8000/docs](https://www.google.com/search?q=http://localhost:8000/docs)



**Креденшіали для доступу в адмін-панель:**

* **Логін:** `admin@test.com`
* **Пароль:** `supersecretpassword`

---

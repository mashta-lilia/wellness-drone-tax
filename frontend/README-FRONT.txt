1. Ідея та Концепція дизайну

Мета: Створення інтуїтивно зрозумілого інтерфейсу для моніторингу та управління податковими нарахуваннями замовлень, зроблених через дрони.

Стилістика: Обрано "чистий" корпоративний стиль (Clean Admin UI). Основний акцент на читабельності даних та простоті навігації.

Колірна палітра:

#f4f6f8 — м'який світло-сірий фон для зменшення навантаження на очі.

#1976d2 (MUI Primary Blue) — для акцентів, кнопок та активних станів.

#ffffff — білі картки (Paper) для візуального розділення блоків.

UX-рішення: Використання бічного меню (Drawer) для швидкого доступу до функцій та Dashboard-карток для миттєвого перегляду статистики.

2. Технологічний стек

Вибір інструментів обґрунтований необхідністю швидкої розробки та суворої типізації:

React 18 + Vite: Найшвидше середовище для збірки та розробки.

TypeScript: Гарантія надійності даних, особливо при обробці складних структур податкових нарахувань.

Material UI (MUI): База компонентів для створення професійного інтерфейсу.

React Router DOM v6: Організація навігації та захищених маршрутів (PrivateRoutes).

Axios: Централізоване управління запитами до API (FastAPI) з перехоплювачами (interceptors) для авторизації.

React Hook Form: Оптимізована робота з формами та валідацією.

React Toastify: Система сповіщень про успіх або помилки операцій.

3. Реалізація функціоналу (Покроково)

🔑 Авторизація та Безпека

Реалізовано сторінку LoginPage з валідацією полів.

Налаштовано PrivateRoute: перевірка JWT-токена в localStorage. Якщо токена немає — автоматичний редирект на /login.

Додано Axios Interceptor: автоматичне підставлення токена в заголовок Authorization: Bearer ... для кожного запиту.

Реалізовано функцію Logout: очищення сховища та миттєве повернення на сторінку входу.

📊 Головна сторінка (Orders List)

Dashboard: Реалізовано три інформаційні картки (SummaryCard) із загальною статистикою: кількість замовлень, сума податків та середня ставка.

Таблиця замовлень:

Відображення ID, координат, суми та податків.

Expandable Row (Розгортання): При кліку на рядок відкривається детальна калькуляція (Breakdown), що показує частку штату, округу та спец. ставки (MCTD).

Сортування та Пагінація: Серверна обробка великої кількості даних через параметри запиту.

Фільтрація: Пошук за ID та вибір конкретної дати через кастомний DatePicker.

➕ Створення замовлення (Manual Create)

Реалізовано форму з миттєвою валідацією координат (latitude/longitude) та суми.

Після успішного створення користувач відразу бачить розрахований чек із податками.

Додано кнопку "Новий розрахунок" для швидкого очищення форми.

📂 Імпорт CSV

Інтегровано Drag-and-Drop зону для завантаження файлів.

Реалізовано клієнтську перевірку розширення (.csv) та розміру файлу (до 5 МБ).

Звіт про імпорт: Після завантаження виводиться статистика (успішно/помилки) та список причин відмови для кожного проблемного рядка.

🛠 Додаткові сервіси

Експорт: Кнопка "Завантажити звіт", що генерує CSV-файл на основі поточних фільтрів.

Очищення бази: Кнопка з модальним вікном підтвердження (Dialog) для повного видалення даних, щоб уникнути випадкових дій.

4. Оптимізація та Чистота коду

Код повністю очищений від "сміттєвих" коментарів.

Додано JSDoc документацію до всіх компонентів, функцій та інтерфейсів.

Реалізовано централізовані утиліти formatters.ts для однакового відображення валют, відсотків та координат по всьому проєкту.
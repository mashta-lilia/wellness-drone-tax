#!/bin/sh

echo "🚀 Запуск міграцій бази даних..."
# Додаємо || true, щоб скрипт не падав, якщо таблиці вже є
python -m alembic upgrade head || echo "⚠️ Міграції вже застосовані або виникла помилка, йдемо далі..."

echo "🌱 Запуск Seeder'а..."
python seed.py

echo "🔥 Запуск сервера FastAPI..."
exec python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
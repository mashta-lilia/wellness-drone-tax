services:
  api:
    build: .
    container_name: drone-tax-backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:////app/wellness_delivery.db
      - PYTHONUNBUFFERED=1
    volumes:
      - ./wellness_delivery.db:/app/wellness_delivery.db
    restart: always